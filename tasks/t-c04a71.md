---
id: t-c04a71
title: Match Clawdia orange and streamline single-server launch
status: done
added: 2026-07-12
---

## Description

Bring the iOS accent palette closer to the brighter pumpkin orange in Clawdia's current app icon without changing the macOS Scarf palette. When exactly one fully configured server exists, connect to it automatically at launch; retain the server picker for multi-server setups and incomplete configurations.

## Plan

See `documents/plans/2026-07-12-clawdia-orange-single-server-launch.md`.

## Verification

- All 20 tests in `Scarf iOSTests` pass on the iOS 26.5 iPhone 17 Pro simulator, including focused coverage for one-server auto-connect, multiple-server picker routing, and incomplete single-server recovery.
- The ScarfDesign package builds successfully for macOS, confirming the existing Scarf rust palette remains valid outside iOS.
- The Clawdia iOS scheme builds successfully in Release configuration for the iOS 26.5 simulator.
- A runtime simulator launch resolves the new pumpkin orange from both the package and app accent catalogs.
- All edited asset-catalog JSON validates with `jq`; `git diff --check` passes.
