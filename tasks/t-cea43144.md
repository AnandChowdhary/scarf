---
id: t-cea43144
title: Next release notes: add project context-file awareness line (t-42db11e9)
status: todo
added: 2026-06-28
priority: low
---

## Description

Queue from t-42db11e9 (decision: release-note awareness, not a trust gate) + t-0b850b5b (reconciled). The next Scarf cut (first release after v2.13.0) ships project-chat context loading: commits b421280 "load project AGENTS.md in ACP chats — spawn hermes acp with cwd=project" and 5538e30 "project cwd for resume/reconnect/auto-start chats" are UNRELEASED (in v2.13.0..main). When that ships, the release notes MUST carry this awareness line, because diff-scoping will render it as a plain feature bullet and lose the trust caution otherwise.

FINAL LINE (chat-only — paste verbatim into the security/awareness area of releases/v<next>/RELEASE_NOTES.md, and reflect a short form in the README "What's New"):

> Opening a chat in a project now loads that project's `AGENTS.md` / `CLAUDE.md` / `.cursorrules` into the agent (so it has project context). Treat a project's context files like its code — only open chats in projects you trust.

SCOPE = CHATS ONLY. Do NOT mention mini-apps. t-0b850b5b shipped option (b): MiniAppAgentSession spawns via the default `{ ACPClient.forMacApp(context:$0) }` factory with no projectCwd, so mini-app agents deliberately do NOT load project context (only their ACP session cwd = projectRoot, for tool dirs). The line must expand to mini-apps ONLY if a future change threads projectCwd into that factory (revisit behind a mini-app context-trust affordance) — guarded in-code by the docstring + clientFactory NOTE in MiniAppAgentSession.swift.

Close this task when the line lands in the next RELEASE_NOTES.md. Full rationale: .memory/decisions/project-context-file-injection-release-note-awareness-not-a.md

## Plan



## Artifacts



