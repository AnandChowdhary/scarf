---
id: t-0b850b5b
title: MiniApp agent sessions: decide process-cwd for AGENTS.md (same root cause as t-565f8d45, trust-gated)
status: done
added: 2026-06-28
priority: low
---

## Description

Surfaced by the t-24594c4a fresh-eyes audit (2026-06-28). `MiniAppAgentSession` (scarf/scarf/Features/Projects/MiniApp/MiniAppAgentSession.swift) spawns its ACP client via the default factory `{ ACPClient.forMacApp(context: $0) }` (line ~53) — NO `projectCwd` — yet sets the ACP `session/new` cwd to the project root (`newSession(cwd: projectRoot)`, line ~145). This is the exact process-cwd vs session-cwd split that t-565f8d45 / t-24594c4a fixed for chats: mini-app agent sessions resolve TOOL dirs against the project but do NOT load the project's AGENTS.md/CLAUDE.md (Hermes reads context files from the PROCESS cwd). The class docstring ("cwd = project root") is now misleading — it conflates the two cwds.

NOT a clear-cut fix — it intersects t-42db11e9 (untrusted project context-file injection). Mini-apps run untrusted / agent-generated web content that drives the agent via `scarf.prompt`; auto-injecting the project's AGENTS.md/.cursorrules into that agent session is a BIGGER prompt-injection surface than an interactive chat the user explicitly opened. So this is a DESIGN decision, not a reflexive one-liner.

Decide one of: (a) thread `projectCwd: projectRoot` into the factory default here too (one line) so mini-app agents get full project context — only if t-42db11e9 lands a trust gate first; (b) leave process cwd = home (no AGENTS.md for mini-apps) as a deliberate trust-minimizing choice, and FIX the docstring to say "ACP session cwd = project root; process cwd intentionally home — AGENTS.md not loaded for mini-app agent sessions (trust)". Either way the misleading docstring must be corrected. Pairs with t-42db11e9.

## Plan

PROMPT for a fresh session (self-contained):
---
Work ticket t-0b850b5b on the Scarf repo (/Users/awizemann/Developer/Scarf — macOS GUI for the Hermes agent). Confirm Memophant MCP is alive; read tasks/t-0b850b5b.md and the related decision note decisions/project-context-file-injection-release-note-awareness-not-a (t-42db11e9) before starting. Branch off main first.

BACKGROUND: Hermes loads a project's context files (AGENTS.md/CLAUDE.md/.cursorrules) from the `hermes acp` PROCESS cwd — NOT the ACP session/new cwd (which it uses only for tool dirs). t-565f8d45 + t-24594c4a fixed Scarf's CHAT sessions to spawn `hermes acp` with cwd=project so AGENTS.md loads. MiniAppAgentSession (scarf/scarf/Features/Projects/MiniApp/MiniAppAgentSession.swift) was deliberately NOT changed: it spawns via the default `{ ACPClient.forMacApp(context: $0) }` factory (no projectCwd, ~line 53) yet sets the ACP session cwd to projectRoot (newSession(cwd: projectRoot), ~line 145). So mini-app agent sessions resolve TOOL dirs against the project but do NOT load the project's AGENTS.md, and the class docstring (~line 8, "cwd = project root") misleadingly conflates process-cwd and session-cwd.

This is a DESIGN decision, not a reflexive fix: mini-apps run untrusted / agent-generated web content that drives the agent via scarf.prompt, so auto-injecting the project's AGENTS.md/.cursorrules into that agent is a BIGGER prompt-injection surface than an interactive chat the user opened (see t-42db11e9, which chose release-note awareness over a trust gate for chats).

DO:
1. DOCSTRING (regardless of the decision): correct MiniAppAgentSession's class docstring so it stops conflating process cwd (AGENTS.md source) and ACP session cwd (tool dirs). State current behavior accurately: process cwd = home → AGENTS.md NOT loaded for mini-app agent sessions; session cwd = project root → tool dirs resolve there.
2. DECIDE the behavior: (a) thread projectCwd into the factory default (`{ ACPClient.forMacApp(context: $0, projectCwd: projectRoot) }`) so mini-app agents get full project context — ONLY if you accept the trust expansion AND update the t-42db11e9 release-note to cover mini-apps; OR (b) keep process cwd = home as a deliberate trust-minimizing choice. Ground it in the t-42db11e9 reasoning (projects are user-chosen, but mini-app web content is less trusted) and whether any mini-app actually needs project context today.

PROCESS (dev cycle): plan → execute → REAL tests (test the function, not just the edit) → adversarial fresh-eyes audit → clean commit. Do NOT push. ScarfCore stays Linux-compilable (no SwiftUI/AppKit); main uses SWIFT_DEFAULT_ACTOR_ISOLATION=MainActor (mark off-main helpers nonisolated). Tests: `swift test --package-path scarf/Packages/ScarfCore` AND `xcodebuild test -project scarf/scarf.xcodeproj -scheme scarf -only-testing:scarfTests -destination 'platform=macOS'` (never bare xcodebuild test). If you change behavior, mirror MiniAppAgentSession's existing test coverage. move_task t-0b850b5b to done when finished, and record in t-42db11e9 whether the release-note line must expand to mention mini-app context loading.
---

## Artifacts



