---
id: t-93517390
title: Project tool/skill scoping (blocked on hermes-agent#45958)
status: todo
added: 2026-06-21
---

## Description

Apply per-project scoped toolsets/skills at ACP session start. `ScarfProject` already carries `scopedToolsets`/`scopedSkills` (currently always empty). BLOCKED UPSTREAM: Hermes's `acp_adapter/session.py:_make_agent` hardcodes `enabled_toolsets=["hermes-acp"]` (+ configured MCP servers) and ignores the `hermes acp -t/--toolsets` flag — there is no per-session toolset seam. Filed NousResearch/hermes-agent#45955 (issue) + #45958 (PR adding `SessionManager(default_toolsets)` + a per-session `toolsets` param on `session/new`). Buildable ONLY once that merges; then thread the project's scoped sets into the ACP `new_session` call and surface a "Scope" panel in the cockpit. From the Phase-1 "projects amazing" plan — Decision #2 (locked 2026-06-14), the one upstream-blocked item.

## Plan

RE-VERIFIED BLOCKED 2026-06-28 against installed Hermes v0.17.0 (upstream ea8facee).

NousResearch/hermes-agent#45958 ("feat(acp): honor --toolsets and add per-session tool scoping") = state OPEN, mergedAt null → NOT merged.

Installed-code confirmation (~/.hermes/hermes-agent/acp_adapter/):
- session.py:558 `_make_agent(*, session_id, cwd, model, requested_provider, base_url, api_mode)` — NO toolsets param; hardcodes enabled_toolsets via `_expand_acp_enabled_toolsets(["hermes-acp"], mcp_server_names=configured_mcp_servers)` (session.py:593-596).
- session.py:210 `create_session(self, cwd: str = ".")` — NO toolsets param.
- server.py:1109 `new_session` → `create_session(cwd=cwd)` (server.py:1115, 1186) — no toolsets threaded from the ACP session/new request.
- server.py:825-841 / 1787 recompute tools from `getattr(state.agent, "enabled_toolsets", None) or ["hermes-acp"]` after MCP registration — INTERNAL recompute keyed off the agent's default, NOT a client-facing setter. No ACP RPC lets a client specify per-session toolsets (enabled or disabled).

NOTE: v0.17 DID refactor toward this (extracted `_expand_acp_enabled_toolsets(toolsets, mcp_server_names)` helper + `disabled_toolsets` getattr hooks), so when #45958 lands the Scarf side is small: thread ScarfProject.scopedToolsets/scopedSkills into the ACP new_session call + surface a "Scope" panel. Re-check #45958's merge status before attempting. STILL BLOCKED — no work done.

## Artifacts



